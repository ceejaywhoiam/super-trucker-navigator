import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ceejaywhoiam.supertruckernavigator',
  appName: 'Super Trucker Navigator',
  webDir: 'dist',
  server: {
    cleartext: false
  }
};

export default config;
